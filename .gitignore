# Dependencies
node_modules/

# Next.js
.next/
out/
next-env.d.ts

# Local data: database, puzzle dumps, engine binaries
data/

# Environment — secrets never belong in the repo.
# `.env.example` is deliberately NOT ignored: it is the template, and carries
# no values.
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.swp

# TypeScript build artifacts
*.tsbuildinfo
